<%= render 'refinery/admin/search_header', :url => refinery.divisions_admin_positions_path %>
<div class='pagination_container'>
  <% if @positions.any? %>
    <%= render 'positions' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
