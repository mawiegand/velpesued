<%= render 'refinery/admin/search_header', :url => refinery.contacts_admin_contacts_path %>
<div class='pagination_container'>
  <% if @contacts.any? %>
    <%= render 'contacts' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
