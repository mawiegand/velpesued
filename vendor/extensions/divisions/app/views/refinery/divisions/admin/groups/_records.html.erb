<%= render 'refinery/admin/search_header', :url => refinery.divisions_admin_groups_path %>
<div class='pagination_container'>
  <% if @groups.any? %>
    <%= render 'groups' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
