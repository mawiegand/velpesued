<% content_for :body do %>
  <ul id="divisions">
    <% @divisions.each do |division| %>
      <li>
        <%= link_to division.name, refinery.divisions_division_path(division) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
