<% content_for :body do %>
  <ul id="groups">
    <% @groups.each do |group| %>
      <li>
        <%= link_to group.name, refinery.divisions_group_path(group) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
